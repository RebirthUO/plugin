Use when implementing or reviewing ModernUO runtime string construction with interpolated-string handlers, Server.Text.ValueStringBuilder, message or gump APIs, packet text, span consumers, or repeated game-code assembly. Route IPropertyList delimiter semantics to modernuo-property-lists.
