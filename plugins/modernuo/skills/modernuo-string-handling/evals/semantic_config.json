{
  "optimizer_hints": {
    "capability": "ModernUO runtime string construction",
    "inputs": ["call site", "candidate overload declarations", "ownership and lifetime", "culture or encoding contract"],
    "trigger_actions": ["verify handler binding", "select ValueStringBuilder strategy", "audit span lifetime", "preserve runtime text behavior"],
    "exclusions": ["IPropertyList semantics", "generic C# strings", "network encoding only", "gump layout only", "broad performance profiling", "broad code audit"],
    "artifacts": ["repository evidence", "construction decision", "ownership boundary", "verification result"]
  },
  "fallback_positive_concepts": ["handler_binding", "builder_lifetime"],
  "positive_concepts": {
    "handler_binding": {"weight": 1.0, "phrases": ["interpolated-string-handler overload", "direct handler binding", "temporary string"]},
    "builder_lifetime": {"weight": 1.0, "phrases": ["ValueStringBuilder", "AsSpan", "capacity strategy", "allocation boundary"]}
  },
  "negative_concepts": {
    "near_neighbor": {"weight": 0.8, "exclusive": true, "phrases": ["IPropertyList", "packet byte encoding", "gump HTML layout", "profiling campaign", "broad code audit"]},
    "explicit_no_action": {"weight": 0.5, "exclusive": true, "phrases": ["advice only", "do not inspect code", "generic console application"]}
  }
}
