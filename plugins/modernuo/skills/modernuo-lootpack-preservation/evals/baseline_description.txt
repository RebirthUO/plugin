Use when editing or migrating ModernUO-based creature loot, especially GenerateLoot() and AddLoot(LootPack.*) calls. Preserves source-derived loot entries unless the user explicitly approves an economy-changing replacement.
